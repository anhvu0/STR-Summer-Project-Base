# Python 3.14 compatible runtime dependencies.
torch==2.12.0
numpy==2.4.6

# SUMO Python client libraries. Keep these aligned with the installed SUMO binary.
traci==1.27.0
sumolib==1.27.0

# Test runner for the bundled tests.
pytest==9.0.3
